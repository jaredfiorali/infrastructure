# This is an example .yamllint file
extends: default  # Extend the default configuration

ignore: |
  **/templates/**

rules:
  # Customize specific rules
  line-length: disable
  document-start: disable
  braces: disable